{
  "name": "sc-domain-landing-page",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "pages:dev": "wrangler pages deploy ./ --project-name=sc-domain-landing-page --branch=staging --commit-dirty=true",
    "pages:deploy": "wrangler pages deploy ./ --project-name=sc-domain-landing-page --branch=main --commit-dirty=true"
  },
  "dependencies": {
  },
  "devDependencies": {
    "wrangler": "^3.20.0"
  }
}
